let numbersss: number[] = [1,2,3];

// Inner loop executes each time the outer loop executes once
for(let i: number=0; i < numbers.length; i++){
    for(let j: number = 0; j<2;j++){
        console.log(numbers[i])
    }
}