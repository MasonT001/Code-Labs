// ** Hoisting **
// Hoisting is when havascript declares variables/functions at the top of their scope regardless of where they are at

// x = 1; //reference error

let x = 0;
const y = 1;
var z = 2;

console.log(z);